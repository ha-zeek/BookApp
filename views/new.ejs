<!DOCTYPE html>
<html lang="en">
	<head>
		<%-include("../partials/bootstrap.ejs") %>
	</head>
	<body>
		<div class="mt-5 pt-4">
			<h1 class="text-center text-3xl font-serif font-semibold">Book App</h1>
			<h2 class="text-center text-2xl font-serif">
				Adding New Books to <% if(currentUser){%><%=currentUser.username%><%}%>!
			</h2>
		</div>
		<br />
		<br />
		<div class="flex text-center justify-center w-auto">
			<form action="/books" method="POST">
				<!-- User: -->
				<input
					type="hidden"
					name="userid"
					value="<%=currentUser.username %>"
					display="none" />
				<div class="text-white">
					Title:
					<input
						class="bg-gray-200 border mb-3 border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
						type="text"
						name="title" />
					<br />
					Author:
					<input
						class="bg-gray-200 border mb-3 border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
						type="text"
						name="author" />
					<br />
					Genre:
					<input
						class="bg-gray-200 border mb-3 border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
						type="text"
						name="genre" />
					<br />
					Pages:
					<input
						class="bg-gray-200 border mb-3 border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
						type="number"
						name="pages" />
					<br />
					Recommended:
					<input
						class="ms-2 text-sm font-medium text-gray-900"
						type="checkbox"
						name="recommended" /><br />
				</div>
				<input
					class="text-white mt-4 font-mono bg-slate-700 hover:bg-slate-800 focus:outline-none focus:ring-4 focus:ring-slate-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center me-2 mb-2"
					type="submit"
					value="Add Book" />
			</form>
		</div>
	</body>
</html>
