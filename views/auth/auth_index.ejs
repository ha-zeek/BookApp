<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
  </head>
  <body>
    <h1>Welcome <% if(currentUser){%><%= currentUser.username %> <%} %></h1>
    <ul>
      <% if(currentUser){%>

        <ul>
          <% for(let i=0;i<books.length;i++) {%>
            <li>
             <a href="/books/<%=books[i].id %>"> <%= books[i].title %></a>
             <a href="/books/<%=books[i].id %>/edit"><button>EDIT</button></a>
             <form action="/books/<%=books[i].id %>?_method=DELETE" method="POST" style="display: inline;">
              <input type="submit" value="DELETE"></form>
            </li><br/>
            <%}%> 
           
        </ul>
        
        <a href="/books/new">Add more books!</a>
      <form action="/sessions?_method=DELETE" method="POST">
        <input type="submit" value="Logout" />
      </form>
      <%} else{%>
      <li>
        <a href="/sessions/new">Login</a>
      </li>
      <li>
        <a href="/user/new">Sign Up </a>
      </li>
      <%}%>
    </ul>
  </body>
</html>
